# primal-code-sdks

Per-target agent SDK tarballs consumed by Primal Code's SDK downloader.
Contents are repackaged public npm packages (e.g. @anthropic-ai/claude-agent-sdk); no Primal source code lives here.
